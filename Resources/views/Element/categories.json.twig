{% trans_default_domain "kalamu" %}
{
{%- for category, elements in categories -%}
    "{{category}}": {
        {% for name, label in elements %}
            "{{name}}": "{{label|trans}}"{{loop.last ? "" : ","}}
        {% endfor %}
    }{{loop.last ? "" : ","}}
{%- endfor -%}
}